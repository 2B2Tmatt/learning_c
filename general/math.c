#include <stdio.h> 
#include <math.h> 

int main(){

    float x = 90; 
    //x = sqrt(x); 
    //x = ceil(x); 
    //x = round(x); 
    //x = sqrt(x); 
    //x = pow(x, 4); 
    //x = abs(x);
    //x = log(x); 
    //x = sin(x); 
    //x = cos(x); 
    //x = tan(x);
    printf("%f", x); 
    
    return 0; 
}